{
	"name": "cv-rebel",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build:css": "tailwindcss -i ./src/styles.css -o ./public/styles.css",
		"dev": "bun run build:css && wrangler dev",
		"start": "bun run build:css && wrangler dev",
		"deploy": "bun run build:css && wrangler deploy",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@tailwindcss/cli": "^4.2.4",
		"@types/node": "^25.6.0",
		"autoprefixer": "^10.5.0",
		"postcss": "^8.5.10",
		"tailwindcss": "^4.2.4",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.84.1"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.90.0",
		"hono": "^4.12.14"
	}
}